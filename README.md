## Dockerized API 

Commands used to generate the image from this project and push it to DockerHub is located inside `dockerhub.sh` script file. The scripts to run compose files are also documented in `compose.sh`.
